const fs = require('fs'); const t = fs.readFileSync('src/models/quotationModel.js','utf8'); const sql = t.match(/\) VALUES \(([^)]+)\)/s); console.log('placeholders', sql ? sql[1].match(/\?/g).length : 'none'); const arr = t.match(/\[\s*\n([\s\S]*?)\n\s*\]/m); if (!arr) { console.log('params none'); process.exit(1);} const params = arr[1].split(/\r?\n/).map(l => l.trim()).filter(l => l.length && !l.startsWith(']')); console.log('params', params.length); console.log(params.join('\n'));
