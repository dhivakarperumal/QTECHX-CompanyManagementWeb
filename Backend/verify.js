const fs = require('fs'); const text = fs.readFileSync('src/models/quotationModel.js','utf8'); const start = text.indexOf('db.execute('); if (start === -1) { console.error('no db.execute'); process.exit(1);} const argsText = text.slice(start + 'db.execute('.length); const firstComma = argsText.indexOf(','); if (firstComma === -1) { console.error('no comma'); process.exit(1);} const sqlText = argsText.slice(0, firstComma); const placeholders = (sqlText.match(/\?/g) || []).length; console.log('placeholders', placeholders); const arrStart = argsText.indexOf('[', firstComma); if (arrStart === -1) { console.error('no arr start'); process.exit(1);} let depth = 0; let i = arrStart; for (; i < argsText.length; i++) { const c = argsText[i]; if (c === '[') depth++; else if (c === ']') { depth--; if (depth === 0) break; }} if (i >= argsText.length) { console.error('arr end not found'); process.exit(1);} const arrText2 = argsText.slice(arrStart + 1, i); let params = []; let cur = ''; depth = 0; for (let j = 0; j < arrText2.length; j++) { const c = arrText2[j]; if (c === ',' && depth === 0) { if (cur.trim()) params.push(cur.trim()); cur = ''; } else { cur += c; if (c === '{' || c === '[') depth++; else if (c === '}' || c === ']') depth--; }} if (cur.trim()) params.push(cur.trim()); console.log('params', params.length); params.forEach((p, idx)=>console.log(idx+1, p));
